'use strict';

requirejs.config({

});


require(['jQuery','bootstrap'], function ($, bs) {

    var app = {
        initialize: function () {
            // Your code here
        }
    };

    app.initialize();
});
